import PlaceholderPage from "@/components/placeholder-page";

export const metadata = { title: "Offers — SareeHub" };

export default function OffersPage() {
  return (
    <PlaceholderPage
      title="Offers"
      description="Special sales and discounts — coming soon."
    />
  );
}
