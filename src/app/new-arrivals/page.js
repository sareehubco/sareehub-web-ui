import PlaceholderPage from "@/components/placeholder-page";

export const metadata = { title: "New Arrivals — SareeHub" };

export default function NewArrivalsPage() {
  return (
    <PlaceholderPage
      title="New Arrivals"
      description="Fresh styles, timeless traditions — the full list is coming soon."
    />
  );
}
